import { StrictMode } from "react";
import ReactDOM from "react-dom";
import React, { Component }  from 'react';
import Slot from "./Slot";

const rootElement = document.getElementById("root");
ReactDOM.render(<Slot />, rootElement);